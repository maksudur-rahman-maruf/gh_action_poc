name: Example Workflow

on: [push]

jobs:
  greeting_job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run custom greeting action
        uses: ./gh_action_poc  # Local path to your action
        with:
          name: 'Test User'
